version: '3.9'

services:
  oracle:
    container_name: oracle
    image: container-registry.oracle.com/database/express:latest
    ports:
      - 1521:1521
      - 5500:5500
    environment:
      - ORACLE_SID=XE
      - ORACLE_PDB=XEPDB1
      - ORACLE_PWD=pas5w0rd!
      - ORACLE_PASSWORD=pas5w0rd!
    volumes:
      - ./data:/opt/oracle/oradata
    networks:
      - oracle-network

networks:
  oracle-network: